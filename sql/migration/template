DROP TABLE IF EXISTS <t>;

CREATE TABLE <t> AS
SELECT
    id::varchar(36),
    -- (jsonb->>'')::varchar AS ,
    -- (jsonb->>'')::bool AS ,
    jsonb_pretty(jsonb)::json AS data
FROM
    ;

ALTER TABLE <t> ADD PRIMARY KEY (id);

CREATE INDEX ON <t> ();

VACUUM ANALYZE <t>;
