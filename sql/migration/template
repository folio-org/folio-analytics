DROP TABLE IF EXISTS <t>;

CREATE TABLE <t> AS
SELECT
    id::varchar(36),
    -- (jsonb->>'_')::varchar AS _,
    -- (jsonb->>'_')::bool AS _,
    jsonb_pretty(jsonb)::json AS data
FROM
    ;

ALTER TABLE <t> ADD PRIMARY KEY (id);

CREATE INDEX ON <t> (_);

VACUUM ANALYZE <t>;
